# PIP - Complete Structure Documentation

## Root Level: `/home/zerwiz/pip/`

| File | Purpose |
|------|---------|
| `justfile` | Extension launcher and build automation — sets `PI_STACK` env var and loads `pi-loader.ts` |
| `README.md` | Project overview, architecture, quick start |
| `package.json` | NPM dependencies |

---

## `.pi/` — Main Workspace

### Root Files

| File | Purpose |
|------|---------|
| `damage-control-rules.yaml` | Safety rules — dangerous command patterns, read-only paths, no-delete paths, path overrides |
| `teams.yaml` | Legacy team definitions — simple team rosters (superseded by `agents/teams.yaml`) |
| `package.json` | NPM dependency management |
| `settings.json` | General project settings |
| `tsconfig.json` | TypeScript compiler configuration |

### Directories

| Directory | Purpose |
|-----------|---------|
| `.context/` | Runtime context metadata |
| `agent-sessions/` | Runtime agent session storage |
| `agents/` | Agent definitions, YAML configs, team rosters, pipeline chains |
| `build_logs/` | Developer build artifacts and review request queue |
| `docs/` | All project documentation (12 active files) |
| `docs/olddocs/` | Legacy reference docs (archived planning, migration summaries) |
| `extensions/` | Extension system (3 subdirectories + README) |
| `npm/` | NPM packages and node_modules |
| `plans/` | Migration and strategy documents |
| `planning/` | Planning documents generated by planner agent |
| `py/` | Python modules (model selection, memory export tests) |
| `reference/` | Backup location for massive file refactors |
| `referencefiles/` | Alternative backup path for developer rewrites |
| `reviews/` | Audit reports from reviewer and plan-reviewer agents |
| `security_audits/` | Security audit findings from red-team agent |
| `themes/` | 11 color theme configurations (JSON) |
| `web_output/` | Browser automation artifacts from bowser agent |

---

## Complete Directory Tree

```
/home/zerwiz/pip/
├── justfile
├── README.md
├── package.json
│
└── .pi/
    ├── damage-control-rules.yaml    # Safety rules (dangerous patterns, path protections)
    ├── teams.yaml                   # Legacy team definitions
    ├── package.json                 # NPM dependencies
    ├── settings.json                # Project settings
    ├── tsconfig.json                # TypeScript config
    │
    ├── .context/
    │   └── session-state.json       # Runtime session state
    │
    ├── agent-sessions/              # Runtime agent session storage
    │
    ├── agents/
    │   ├── README.md                # Agent system documentation
    │   ├── CHECKLIST.md             # Agent creation checklist
    │   ├── agents.yaml              # Master agent registry (30 agents)
    │   ├── teams.yaml               # Team rosters (13 teams)
    │   ├── agent-chain.yaml         # Pipeline chains (14 chains)
    │   ├── session-manager.yaml     # Session workflows (2 workflows)
    │   ├── reviewer.yaml            # Standalone agent config (model + prompt)
    │   ├── agenttemplate.md         # Template for new agents
    │   │
    │   ├── scout.md                 # Scout agent
    │   ├── planner.md               # Planner agent
    │   ├── developer.md             # Developer agent
    │   ├── reviewer.md              # Reviewer agent
    │   ├── documenter.md            # Documenter agent
    │   ├── red-team.md              # Red team agent
    │   ├── session-manager.md       # Session manager agent
    │   ├── frontendcoder.md         # Frontend coder agent
    │   ├── bowser.md                # Bowser (browser) agent
    │   ├── ext-builder.md           # Extension builder agent
    │   ├── plan-reviewer.md         # Plan reviewer agent
    │   ├── netlify.md               # Netlify agent
    │   ├── netlify-agent.md         # Netlify agent (duplicate)
    │   ├── pi-dev-expert.md         # Pi dev expert agent
    │   │
    │   ├── agents/                  # Homepage team agent definitions
    │   │   ├── accessibility-agent.md
    │   │   ├── content-agent.md
    │   │   ├── design-agent.md
    │   │   ├── dev-agent.md
    │   │   ├── expert-agent.md
    │   │   ├── image-agent.md
    │   │   ├── marketing-agent.md
    │   │   ├── research-agent.md
    │   │   └── seo-agent.md
    │   │
    │   ├── pi-pi/                   # Pi-Pi meta expert definitions
    │   │   ├── agent-expert.md
    │   │   ├── cli-expert.md
    │   │   ├── config-expert.md
    │   │   ├── ext-expert.md
    │   │   ├── keybinding-expert.md
    │   │   ├── pi-orchestrator.md
    │   │   ├── prompt-expert.md
    │   │   ├── skill-expert.md
    │   │   ├── theme-expert.md
    │   │   └── tui-expert.md
    │   │
    │   ├── homepageteam/            # Homepage team configs
    │   ├── special agents/          # Special agent definitions
    │   └── util/                    # Agent utilities
    │       ├── memory-export.ts
    │       └── memory-tools.ts
    │
    ├── docs/
    │   ├── README.md                # Documentation library index
    │   ├── STRUCTURE.md             # This file
    │   ├── AGENT-YAML-CONFIGURATION.md  # YAML config documentation
    │   ├── JUSTFILE-STARTUP-MECHANISM.md # Justfile startup docs
    │   ├── UI-EXTENSIONS-USAGE.md   # src/ui/ usage map
    │   ├── UTIL-EXTENSIONS-USAGE.md # util/ usage map
    │   ├── MAIN-UI-EXTENSIONS-USAGE.md # ui/ usage map
    │   ├── COMBINED-EXTENSIONS-USAGE.md # Combined extension overview
    │   ├── AGENT-TEAM-CHAIN-TEST.md # agent-team-chain docs
    │   ├── MIGRATION-GUIDE.md       # Architecture migration guide
    │   ├── PLAN-MODE.md             # Plan mode feature docs
    │   └── UPDATE-2026-05-02.md     # System update log
    │
    │   └── olddocs/                 # Legacy reference docs (archived)
    │       ├── planning/            # Historical planning documents
    │       ├── ext_builder/         # Extension builder reference
    │       └── *.md                 # Migration summaries, legacy guides
    │
    ├── build_logs/                  # Developer artifacts and review queue
    │   └── review_requests.md       # Developer→Reviewer handoff queue
    │
    ├── planning/                    # Planning documents (planner agent output)
    ├── reviews/                     # Audit reports (reviewer agent output)
    ├── reference/                   # Backup for massive refactors
    ├── referencefiles/              # Alternative backup path
    ├── security_audits/             # Red-team audit findings
    ├── web_output/                  # Browser automation artifacts (bowser)
    │
    ├── extensions/
    │   ├── README.md                # Extension system documentation
    │   ├── CHECKLIST.md             # Extension operational checklist (Pi 0.70.5+ updated)
    │   │
    │   ├── src/ui/                  # Base layer — shared UI components (10 files)
    │   │   ├── themeMap.ts          # Theme definitions (universal dependency)
    │   │   ├── theme-cycler.ts      # Theme cycling commands
    │   │   ├── pure-focus.ts        # Focus mode toggle
    │   │   ├── minimal.ts           # Minimal extension loader
    │   │   ├── tool-counter.ts      # Tool usage tracking
    │   │   ├── tool-counter-widget.ts # Tool counter widget
    │   │   ├── conversation-viewer.ts # Conversation history viewer
    │   │   ├── conversation-viewer.test.ts # Viewer tests
    │   │   ├── manager.ts           # UI manager
    │   │   └── agent-team-chain.ts  # Deprecated wrapper for agent-team
    │   │
    │   ├── util/                    # Middle layer — utilities (12 files)
    │   │   ├── pi-loader.ts         # Dynamic extension loader (orchestrator)
    │   │   ├── manifest.ts          # Extension categories and metadata
    │   │   ├── types.ts             # Shared type definitions
    │   │   ├── memory.ts            # Memory inspection utilities
    │   │   ├── memory-export.ts     # Memory export (JSON, text, markdown)
    │   │   ├── memory-tools.ts      # Memory handling tools
    │   │   ├── damage-control.ts    # Error recovery and safety auditing
    │   │   ├── agent-switch.ts      # Agent switching implementation
    │   │   ├── purpose-gate.ts      # Purpose gate / intent declaration
    │   │   ├── system-select.ts     # System/persona selection
    │   │   ├── session-replay.ts    # Session timeline replay
    │   │   └── justfilechain        # Chain validation script
    │   │
    │   └── ui/                      # Entry layer — primary extensions (6 files)
    │       ├── agent-team.ts        # Agent team management + memory export
    │       ├── agent-chain.ts       # Sequential pipeline orchestrator
    │       ├── cross-agent.ts       # Cross-agent command/skill discovery
    │       ├── pi-pi.ts             # Meta-agent with parallel research
    │       ├── subagent-widget.ts   # Background subagents with live widgets
    │       └── tilldone.ts          # Task discipline system
    │
    ├── npm/
    │   ├── package.json
    │   ├── package-lock.json
    │   ├── .gitignore
    │   └── node_modules/
    │
    ├── plans/
    │   ├── planmode for agent-team.ts.md
    │   ├── RECONFIGURATION-PLAN.md
    │   └── Z_AI_TO_PI_AGENT_MIGRATION.md
    │
    ├── py/
    │   ├── model_resolver.py        # Model resolution logic
    │   ├── model_selector.py        # Model selection logic
    │   ├── models.py                # Core model definitions
    │   ├── prompt_templates.py      # Prompt templates
    │   ├── test_memory_export.py    # Memory export tests
    │   └── test_openai_model_filtering.py # Model filtering tests
    │
    └── themes/
        ├── catppuccin-mocha.json
        ├── cyberpunk.json
        ├── dracula.json
        ├── everforest.json
        ├── gruvbox.json
        ├── midnight-ocean.json
        ├── nord.json
        ├── ocean-breeze.json
        ├── rose-pine.json
        ├── synthwave.json
        └── tokyo-night.json
```

---

## Extension Boot Strategy

### How Extensions Are Loaded

All extensions (except root-level) are loaded via the justfile using a **single-entry dynamic loader**:

```bash
just ext-full-stack
  → PI_STACK="agent-team,damage-control,theme-cycler"
  → pi -e .pi/extensions/util/pi-loader.ts
  → pi-loader.ts resolves, categorizes, and initializes each extension
```

### Extension Layers

| Layer | Directory | Files | Role |
|-------|-----------|-------|------|
| Entry | `ui/` | 6 | Primary entry-point extensions |
| Middle | `util/` | 12 | Utilities, memory, loaders |
| Base | `src/ui/` | 10 | Shared UI components, themes |

### Path Resolution Order

When `pi-loader.ts` looks for an extension by name, it searches:

```
1. extensions/<name>.ts
2. extensions/<name>.js
3. .pi/extensions/<name>.ts
4. .pi/extensions/ui/<name>.ts
5. .pi/extensions/util/<name>.ts
6. .pi/extensions/src/ui/<name>.ts
```

---

## Key Files Explained

### `justfile` (root)

The single entry point for running Pi with extensions. Defines ~25 targets that set `PI_STACK` and load `pi-loader.ts`. Never pass multiple `-e` flags directly — use the justfile.

### `.pi/damage-control-rules.yaml`

Safety configuration with four rule categories:

| Section | Purpose | Entries |
|---------|---------|---------|
| `bashToolPatterns` | Dangerous command patterns to block or flag | 60+ patterns (rm, git, aws, gcloud, SQL, etc.) |
| `readOnlyPaths` | Paths that agents can read but not modify | 50+ paths (.env, SSH keys, cloud creds, lock files, build dirs) |
| `noDeletePaths` | Paths that cannot be deleted | 20+ paths (.git, LICENSE, README, CI configs, Dockerfile) |
| `pathOverrides` | Highest-priority per-path permission rules | 7 active overrides |

Patterns marked with `ask: true` prompt the user before executing. Patterns without `ask` are blocked entirely.

### `.pi/teams.yaml`

Legacy team definitions — simple lists of agent names. **Superseded by** `.pi/agents/teams.yaml` which has more detailed team configurations (13 teams vs 6 here). Kept for backward compatibility.

| Team | Members |
|------|---------|
| `main` | scout, planner, developer, reviewer, documenter, red-team, session-manager |
| `plan-build` | planner, developer, reviewer, session-manager |
| `info` | scout, documenter, reviewer, session-manager |
| `frontend` | planner, frontend-coder, scout, reviewer, bowser, session-manager |
| `pi-pi` | ext-expert, theme-expert, skill-expert, config-expert, tui-expert, prompt-expert, agent-expert, session-expert |
| `Extra` | developer, reviewer, scout, dispatcher |

---

## File Type Summary

| Extension | Count | Purpose |
|-----------|-------|---------|
| `.yaml` | 7 | Agent configs, teams, chains, safety rules |
| `.md` | 35+ | Agent definitions, documentation |
| `.ts` | 28 | TypeScript extensions and utilities |
| `.py` | 6 | Python model and test modules |
| `.json` | 14 | Theme configs, package files, settings |
| `.json` (themes) | 11 | Color theme definitions |

---

## Quick Navigation

| Need | Location |
|------|----------|
| Run Pi with extensions | `justfile` (root) |
| Agent definitions | `.pi/agents/` |
| Agent YAML configs | `.pi/agents/*.yaml` |
| Extensions | `.pi/extensions/` |
| Extension docs | `.pi/extensions/README.md` |
| All project docs | `.pi/docs/` |
| Doc index | `.pi/docs/README.md` |
| Safety rules | `.pi/damage-control-rules.yaml` |
| Themes | `.pi/themes/` |
| Python modules | `.pi/py/` |
| Plans | `.pi/plans/` |
